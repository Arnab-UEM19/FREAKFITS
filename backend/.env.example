# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=freakfits_db

# Security & JWT Configuration
JWT_SECRET_KEY=freakfits_super_secret_jwt_key_2026_change_in_production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# SMTP Email Configuration (e.g. Gmail / Outlook / Custom SMTP)
# For Gmail: Use your full email as SMTP_USER and a 16-character Google "App Password" as SMTP_PASSWORD
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME="FreakFits Official"

# Server Configuration
PORT=8000
HOST=0.0.0.0
CORS_ORIGINS=["*"]
